# Security policy

Do not publish secrets, credentials, private data or weaponized exploit payloads in public issues.

Security findings remain scoped:
`static finding != feasible exploit != observed exploitation`.
