# CODE-24 validation

Validation date: **2026-09-08**

Status: **24/24 repository hubs resolved with read/pull access through the connected GitHub integration.**

The registry is a discovery/adapter fabric, not a bulk installer. Public upstream repositories are read-only from this connection unless the authenticated maintainer has push rights on a fork/project repository.

Bash uses `tianon/mirror-bash` and is explicitly classified as a maintained mirror rather than an official GitHub upstream.

The remaining entries resolve to their listed project/upstream repositories. Runtime compiler/tool availability is a separate concern and must be capability-admitted per task.
