# Governance

The public core follows evidence-first and human-authority invariants.

Changes that weaken evidence provenance, authority boundaries, fail-closed behavior, deterministic receipts, or rollback semantics require explicit review and dedicated regression tests.

The default branch is treated as release-facing state. Experimental architecture belongs on review branches until tests and evidence justify promotion.
